* Added a new setting in the FOV group called "FOV" to set the field of view.
* Fixed an issue where the game would be light blue if it was minimised while loading.
* Replaced the "Adjust Field of View" setting with with a new setting in the FOV group called "Mode".
	* Defaults to Fit.
	* Lock Horizontal is the same as the old setting being disabled while Lock Vertical is the same as when it was enabled.