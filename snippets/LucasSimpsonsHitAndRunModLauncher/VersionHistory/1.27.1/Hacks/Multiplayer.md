* Made the "Failed to start connecting" message shown in the console more informative.
* Fixed an issue where a console window would appear even if the **Console and Logging** hack was not enabled.
* Made the hack check once on startup if the **Mirror Mode** hack is also enabled instead of every frame.
	* This makes nametag rendering more efficient.
* Made it so the chat box handles overflow with really long names.
* Made it so you will only hear suspension sounds for your own vehicle.
* Fixed a possible crash when certain HUD icons were updated at the server's request.